const actions = {
	lobby: {
		create: "create:lobby",
	},
} as const;

// this will parse the action then pass them on.
class websocketHandler {
	// handle incoming request
	// send outgoing request
}

export { actions };
